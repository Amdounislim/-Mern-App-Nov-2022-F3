{
  "scripts": {
    "front":"cd front && npm start",
    "back": "cd back && nodemon server.js",
    "dev": "concurrently \"npm run front\" \"npm run back\" "
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
